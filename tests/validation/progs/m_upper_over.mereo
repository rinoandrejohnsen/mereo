include "../../../linux.mereo"
include "../../../core.mereo"

-- upper walks past the end of what it was given
program goes
  buf is 32 bytes
  terminal is already linux.file (descriptor is 1)
  text.upper (data is buf, length is 64)
  terminal.write (buffer is buf, count is 32)
end
