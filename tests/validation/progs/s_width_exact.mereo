include "../../../linux.mereo"

-- an eight-byte load whose last byte is the buffers last
program goes
  buf is 16 bytes
  d is 0
  terminal is already linux.file (descriptor is 1)
  d is [buf + 8 : 8]
  [buf : 1] is d
  terminal.write (buffer is buf, count is 1)
end
