include "../../../linux.mereo"
include "../../../core.mereo"

-- text.fill past the target
program goes
  dst is 32 bytes
  terminal is already linux.file (descriptor is 1)
  text.fill (target is dst, length is 48, byte is 65)
  terminal.write (buffer is dst, count is 32)
end
