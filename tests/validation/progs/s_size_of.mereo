include "../../../linux.mereo"

-- the buffer states its own size, and the loop uses it
program goes
  buf is 16 bytes
  i is 0
  d is 0
  terminal is already linux.file (descriptor is 1)
  walk goes
    leave walk when i >= buf.size
    d is d + [buf + i : 1]
    i is i + 1
    repeat walk
  end
  [buf : 1] is d
  terminal.write (buffer is buf, count is 1)
end
