include "../../../linux.mereo"
include "../../../core.mereo"

-- upper walks the length it is given
program goes
  buf is 32 bytes
  terminal is already linux.file (descriptor is 1)
  text.upper (data is buf, length is 32)
  terminal.write (buffer is buf, count is 32)
end
