include "markers.mereo"

-- A fault while a block-local resource is live must release it, then the outer.
-- Expect close(101) close(103).
program goes
  keep is adopted mark_c (descriptor is 103)
  g is 0
  scope
    tmp is adopted mark_a (descriptor is 101)
    ensure g >= 1
  end
end
